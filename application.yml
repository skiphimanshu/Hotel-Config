spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userhotelrating
    username: root
    password: Himanshu
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
